{
	"name": "cns",
	"description": "Clone 'n' Start - A simple tool which clones a branch from a repository, runs npm install, grunt, and starts the server with forever (and stops the previous if that was running).",
	"version": "0.0.2",
	"bin": {
		"cns": "cns.js"
	},
	"author": "Gyula Németh",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/EDMdesigner/cns.git"
	},
	"keywords": ["build", "git", "clone", "grunt", "forever"],
	"dependencies": {
		"nconf": "0.6.9",
		"dateformat": ""
	}
}
